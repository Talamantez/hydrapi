DEPLOY_URL=https://web-production-6c068.up.railway.app
DEPLOY_API_KEY=test-key-123
```

2. Run specific environment tests:
```bash
# Run local tests only
pytest -v -k "local"

# Run deployment tests only
pytest -v -k "deploy"

# Run all tests
pytest -v